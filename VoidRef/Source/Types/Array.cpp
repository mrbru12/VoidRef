#include "Array.h"

/*
namespace VRef
{
    template <class T>
    Array<T>::Array(size_t arraySize, T* arrayBegin)
        : size(arraySize), begin(arrayBegin)
    {
    }

    template <class T>
    T& Array<T>::operator[](unsigned int index)
    {
        return begin[index];
    }
}
*/
